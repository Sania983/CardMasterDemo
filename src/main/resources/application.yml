spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db9?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: TIGER
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect   # <-- tells mysql is used
    hibernate:
      ddl-auto: update                                      # <--  data definition language(sql)
    properties:
      hibernate:
        format_sql: true                                    #to follow sql format

server:
  port: 8082